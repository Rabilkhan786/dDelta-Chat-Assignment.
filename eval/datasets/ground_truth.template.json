{
  "_comment": "Copy this file to ground_truth.json and fill it in by hand after reviewing a real run.",
  "expected_change_ids": [
    "_comment: run `uv run python -m eval.run_eval --write-candidates`, review eval/datasets/predicted_change_ids.json against the delta report, then paste the IDs you confirm are real changes here"
  ],
  "qa_cases": [
    {
      "question": "What changed near the compressor?",
      "expected_answer_keywords": ["compressor"],
      "expected_citations": ["delta_report | PID revision_b | page 1 | delta-1"],
      "expected_retrieval_ids": ["delta-1"]
    }
  ],
  "known_failure_cases": [
    "Describe where the system is known to fall short, for example moved content that alignment fails to match."
  ]
}
